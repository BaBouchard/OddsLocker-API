# Terminal - central aggregator for scrapers
# Copy to .env and set as needed

# Port for HTTP (POST /ingest) and WebSocket (viewers). Railway sets PORT.
PORT=3000

# Optional: same value on scraper (TERMINAL_INGEST_SECRET) so only your VPS can POST /ingest.
# TERMINAL_INGEST_SECRET=

# Optional: password for viewing the terminal page + WebSocket (does not block /ingest).
# TERMINAL_LOGIN_PASSWORD=

# WebSocket readers: comma-separated secret tokens (one per customer / API key). Do not use commas inside a token.
# Clients: wss://host/?token=YOUR_TOKEN  OR  Authorization: Bearer YOUR_TOKEN
# If set, WebSocket requires a valid token OR (when TERMINAL_LOGIN_PASSWORD is set) the ol_auth cookie after login.
# TERMINAL_WS_ALLOWED_TOKENS=ol_live_abc123,ol_live_xyz789

# Troubleshooting: if 1/true, each ingest wipes all other VPS snapshots so the feed shows only the latest request.
# Leave unset for normal multi-VPS merge.
# TERMINAL_REPLACE_ALL_ON_INGEST=
