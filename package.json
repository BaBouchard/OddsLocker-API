{
  "name": "live-odds-ingestion",
  "version": "1.0.0",
  "description": "High-speed WebSocket service to ingest live game odds from a sportsbook for arbitrage integration",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "build:bundle": "esbuild src/index.js --bundle --platform=node --target=node18 --format=cjs --outfile=dist/scraper-bundle.cjs",
    "build:win": "npm run build:bundle && pkg dist/scraper-bundle.cjs --targets node18-win-x64 --output dist/live-odds-scraper.exe"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.14.1",
    "esbuild": "^0.27.4"
  }
}
